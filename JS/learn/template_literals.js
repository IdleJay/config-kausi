/*
let temp = 23
let place = 'Edinburgh'

// console.log("Today it's " + temp + " degrees C here in " + place)
//Template literals- instead of using the inverted commas, we'll use the back ticks
console.log(`Today it's ${temp} degrees C here in ${place}`)
*/

let noMultiLine = "No multi-line strings in ESS";
console.log("Did you know? " + noMultiLine);